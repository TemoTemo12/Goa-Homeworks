// const arr = ['apple', 'banana', 'orange', 'grape', 'peach'];

// // map - თითოეული ელემენტის მანიპულირება, მაგალითად, თითოეულ სიტყვას დავამატოთ 'fruit'
// const mappedArr = arr.map(item => item + ' fruit');
// console.log(mappedArr);

// // filter - მხოლოდ "a" ასოთი დაწყებული ელემენტების ამოღება
// const filteredArr = arr.filter(item => item.startsWith('a'));
// console.log(filteredArr);

// // find - კონკრეტული ელემენტის პოვნა, მაგალითად, ელემენტი, რომელიც შეიცავს "grape"
// const foundItem = arr.find(item => item === 'grape');
// console.log(foundItem);

// // findIndex - კონკრეტული ელემენტის ინდექსის პოვნა, მაგალითად, 'orange'
// const foundIndex = arr.findIndex(item => item === 'orange');
// console.log(foundIndex);

// // join - მასივის ელემენტების გაერთიანება ერთ სტრინგში, სეპარატორით
// const joinedArr = arr.join(', ');
// console.log(joinedArr);

// // indexOf - კონკრეტული ელემენტის ინდექსის პოვნა
// const indexOfItem = arr.indexOf('peach');
// console.log(indexOfItem);

// // startsWith - მაგალითად, შემოწმება, თუ ელემენტი 'apple' იწყება 'a'-თი
// const startsWithA = arr[0].startsWith('a');
// console.log(startsWithA);

// // endsWith - მაგალითად, შემოწმება, თუ ელემენტი 'banana' მთავრდება 'a'-ზე
// const endsWithA = arr[1].endsWith('a');
// console.log(endsWithA);

// // includes - შემოწმება, შეიცავს თუ არა მასივი კონკრეტულ ელემენტს
// const includesOrange = arr.includes('orange');
// console.log(includesOrange);



// const arr = ['3', '5', '8'];
// console.log(
//   arr.find(x => x == 8).repeat(2)
// );
